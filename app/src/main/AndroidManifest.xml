<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.graduation.wordbookp2">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/> // 내부 파일 접근 권한
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_ic"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WbShareMain" />
        <activity android:name=".WbShareUpload" />
        <activity android:name=".Game" />
        <activity android:name=".Quiz" />
        <activity android:name=".Siteconnection" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Wordsearch" />
        <activity android:name=".Wordbook"></activity>
        <activity android:name=".WBmanage"></activity>
    </application>

</manifest>